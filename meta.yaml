# cmd: conda build .
package:
    name: phycon
    version: "0.1.1"

source:
    path: .

requirements:
    build:
        - python
    run:
        - python
        - scipy
        - pytest
        - pytest-cov
        - flake8
        - pylint
